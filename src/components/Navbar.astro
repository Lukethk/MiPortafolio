---
import { FaHome, FaUser, FaCode, FaTools } from 'react-icons/fa';

const navItems = [
  { name: 'Inicio', href: '#home', icon: FaHome },
  { name: 'Sobre Mí', href: '#about', icon: FaUser },
  { name: 'Proyectos', href: '#projects', icon: FaCode },
  { name: 'Habilidades', href: '#skills', icon: FaTools },
];
---

<nav class="fixed bottom-4 sm:bottom-auto sm:top-6 left-1/2 transform -translate-x-1/2 z-50 w-[90%] sm:w-auto">
  <div class="backdrop-blur-lg bg-white/10 rounded-full border border-white/20 p-1.5 sm:p-2">
    <ul class="flex items-center justify-between sm:justify-start gap-1 sm:gap-2">
      {navItems.map((item) => (
        <li class="flex-1 sm:flex-none">
          <a
            href={item.href}
            class="flex flex-col sm:flex-row items-center justify-center gap-1 sm:gap-2 px-2 sm:px-4 py-2 rounded-full text-white/70 hover:text-white transition-colors relative group text-center"
          >
            <item.icon className="w-5 h-5 sm:hidden" />
            <span class="text-[10px] sm:text-base relative">
              {item.name}
              <span class="absolute inset-0 bg-gradient-to-r from-primary-500/20 to-accent-500/20 rounded-full opacity-0 group-hover:opacity-100 transition-opacity -z-10"></span>
            </span>
          </a>
        </li>
      ))}
    </ul>
  </div>
</nav>

<style>
  nav {
    animation: slideUp 0.5s ease-out forwards;
  }

  @media (min-width: 640px) {
    nav {
      animation: slideDown 0.5s ease-out forwards;
    }
  }

  @keyframes slideUp {
    from {
      transform: translate(-50%, 100%);
      opacity: 0;
    }
    to {
      transform: translate(-50%, 0);
      opacity: 1;
    }
  }

  @keyframes slideDown {
    from {
      transform: translate(-50%, -100%);
      opacity: 0;
    }
    to {
      transform: translate(-50%, 0);
      opacity: 1;
    }
  }
</style>